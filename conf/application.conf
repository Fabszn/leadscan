play.crypto.secret = "V5PLu=uXsP3>b`1yTZpgsKuXm2tIxC5GT7F?_z:0<EIzBK;SoryT/dlE][4QpT_G"


db_leadTrackerDb {
  driver = "org.postgresql.Driver"
  migration.outOfOrder = true
  migration.auto = true
  pool = "bonecp"
  bonecp.logStatements = false
  bonecp.minConnectionsPerPartition = 1
  bonecp.maxConnectionsPerPartition = 2
}

db_server_url = "jdbc:postgresql://"${?POSTGRESQL_ADDON_HOST}":"${?POSTGRESQL_ADDON_PORT}

db {

  leadTrackerDb = ${db_leadTrackerDb} {
    url = ${db_server_url}"/"${?POSTGRESQL_ADDON_DB}
    username = ${?POSTGRESQL_ADDON_USER}
    password = ${?POSTGRESQL_ADDON_PASSWORD}

  }

  #default.logSql=true
}


play.application.loader = LeadTrackerApplicationLoader
play.modules.enabled += org.flywaydb.play.PlayModule

play.filters.cors.allowedOrigins = null

export {
  headers.sponsor = "Rep_first_Name|Rep_last_Name|RegId|first_Name|last_Name|Email_Address|Company|Country|Title|nbNote|allNotes"
  headers.events = "Rep_first_Name|Rep_last_Name|RegId|first_Name|last_Name|Email_Address|Company|Country|Title|nbNote|allNotes|nbNote"
}


oAuth {
  url.base = "http://my.devoxx.fr"
  endpoints {
    auth = "/pwa/signin"
    userinfo = "/pwa/user"
  }
  jwt {
    sharedsecret = ${? HARED_SECCRET}
  }
}


session {
  timeout.mn = 10
}

include "local.conf"
